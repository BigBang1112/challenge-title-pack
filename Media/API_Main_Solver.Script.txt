***Start***
***
while(True) {
	declare SolverFound = False;

	foreach(Event,PendingEvents) {
		switch(Event.Type) {
			case CMlScriptEvent::Type::PluginCustomEvent: {
				switch(Event.CustomEventType) {
					case "Solver": {
						SolverFound = Solver_Problem.fromjson(Event.CustomEventData[0]);
						Solver_Solution = TextLib::ToInteger(Event.CustomEventData[1]);
					}
					case "Hide": {
						SendCustomEvent("Hide_Response", []);
					}
				}
			}
		}
	}

	if(SolverFound) {
		+++Solver+++
		break;
	}

	yield;
}

Solve(Solver_Problem);
***